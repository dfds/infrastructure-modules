# Managed by Terraform Cloud - do not edit
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: platform-apps
  namespace: flux-system
spec:
  interval: 5m
  ref:
    branch: ${flux_platform_apps_branch}
  url: ${flux_platform_apps_url}

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform-apps
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: platform-apps
  path: ${flux_platform_apps_path}
  prune: true
  postBuild:
    substitute:
      flux_aws_account_id: ${flux_aws_account_id}
      flux_aws_region: ${flux_aws_region}
      flux_cluster_name: ${flux_cluster_name}
