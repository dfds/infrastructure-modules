kube-state-metrics:
  resources:
    requests:
      cpu: 20m
      memory: 100Mi
  priorityClassName: ${kube_state_metrics_priorityclass}
  prometheus:
    monitor:
      enabled: true
      additionalLabels:
        release: monitoring
  podSecurityPolicy:
    enabled: true
